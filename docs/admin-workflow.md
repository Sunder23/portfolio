# Admin workflow

Как редактировать контент сайта (проекты, профиль, скиллы, картинки) через встроенную браузерную админку — без бэкенда и без базы данных.

## Доступ

- Скрытый роут: `/#/admin`.
- Точка входа — `TokenGate` (`app/src/admin/TokenGate.tsx`): вводишь fine-grained Personal Access Token с правами только на этот репозиторий (`Contents: Read/Write`).
- Токен проверяется запросом к `GET https://api.github.com/repos/{owner}/{repo}`, а не просто сохраняется вслепую.
- Токен хранится только в `localStorage` — никогда не попадает в код, бандл или коммиты. Кнопка «выйти» очищает `localStorage` и возвращает на `TokenGate`.
- Если API отвечает `401` (истёкший/невалидный токен) — админка сбрасывает на `TokenGate` заново, а не падает молча и не зацикливается.

## Редактирование сущностей

Каждая сущность контента — это тройка «JSON-файл в `app/data/` + тип в `types.ts` + редактор в `app/src/admin/editors/`», зарегистрированная в `app/src/admin/registry.ts`:

- **Проекты** — CRUD (создание, редактирование, удаление, сортировка по `order`), markdown-поле описания с превью.
- **Profile** (`data/profile.json`) — имя, титул, био (markdown), контакты, соцсети.
- **Skills** (`data/skills.json`) — стек и категории.

Новая сущность (блог, отзывы, сертификаты) добавляется без изменения общего кода админки — просто новая запись в `registry.ts` + свой редактор.

## Загрузка изображений

- Файл сжимается прямо в браузере через `<canvas>` в **webp** (до ≤300 КБ) перед отправкой.
- GitHub Contents API ограничивает PUT примерно 1 МБ на файл — сжатие обязательно, а не опционально.
- Загруженные картинки пишутся в `app/public/uploads/` тем же механизмом сохранения, что и JSON (см. ниже).

## Как устроено сохранение (GitHub Contents API)

Подробности — `.claude/skills/portfolio-admin-github-write`. Единственная точка записи — `app/src/admin/github.ts`.

Обязательный порядок для любого сохранения:

1. `GET /repos/{owner}/{repo}/contents/{path}` → взять текущий `sha`.
2. `PUT /repos/{owner}/{repo}/contents/{path}` с телом `{ message, content: base64(...), sha }`.
3. При ответе **409** (sha устарел, файл изменили параллельно) — повторить шаги 1–2 **один раз**. Если и повторная попытка даёт 409 — показывается явная ошибка («файл изменён параллельно, обновите страницу»), без тихого отката и без зацикливания на ретраях.
4. После успешного сохранения — тост «Сохранено. Деплой займёт ~1–2 минуты».

## ⚠️ Та же оговорка про `main`

Каждое сохранение из админки — это **коммит напрямую в `main`**, ту же ветку, в которую пушится и локальная разработка. Это значит, что параллельная локальная сессия разработки может закоммитить/запушить что-то, не зная о правках, только что сделанных через админку (и наоборот).

Правило и объяснение — в [dev-workflow.md](./dev-workflow.md) (раздел «Главная оговорка про `main`»): перед началом любой локальной сессии разработки всегда делать `git pull origin main`.

## См. также

- [dev-workflow.md](./dev-workflow.md) — локальная разработка и деплой
- `.claude/skills/portfolio-admin-github-write` — детальный алгоритм сохранения и обработки ошибок
- `.ai-factory/ARCHITECTURE.md` — граница модуля `admin/` и правила зависимостей
